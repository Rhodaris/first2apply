{
  "name": "first2apply",
  "version": "1.10.1",
  "description": "## Init project `npx supabase init`",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^latest",
    "supabase": "^1.226.4"
  },
  "dependencies": {
    "@electron/asar": "^latest",
    "glob": "^latest"
  }
}
